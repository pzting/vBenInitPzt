export enum PageEnum {
  // basic login path
  BASE_LOGIN = "/login",
  // basic home path
  // BASE_HOME = "/business/salary",
  BASE_HOME = "/system/role",
  // error page path
  ERROR_PAGE = "/exception",
  // error log page path
  ERROR_LOG_PAGE = "/error-log/list",
}
